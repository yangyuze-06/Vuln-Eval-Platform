# 路线图

## 下一步计划
- **SQL 注入 (SQLi)** 建模。
- **服务端请求伪造 (SSRF)** 建模。
- 支持 **反序列化漏洞 (Insecure Deserialization)**。

## 未来方向
- **框架扩展**：增加对 Spring Boot 注解（`@RequestParam`, `@PathVariable`）的支持。
- **精度调优**：实现上下文敏感的堆建模，以减少误报 (FP)。
- **清洗器库**：扩展默认的安全验证模式集。
